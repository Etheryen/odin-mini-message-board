extends layout

block content
  h1= title
  p Welcome to #{title}
  h2 Messages:
  a(href='/new') Add new
  ul 
    each message in messages
      li
        big
          b= message.text
        br
        i= 'by ' + message.user
        br
        small= 'added ' + message.added.toLocaleString()
